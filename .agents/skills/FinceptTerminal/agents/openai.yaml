interface:
  display_name: "FinceptTerminal"
  short_description: "Repo-specific patterns and workflows for FinceptTerminal"
  default_prompt: "Use the FinceptTerminal repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true